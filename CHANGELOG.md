# Changelog

This repo uses a bare incrementing integer version marker (`v1`, `v2`, …),
not SemVer. The marker lives in `package.json`'s `"version"` field.

## v1 — first production marker (2026-06-09)

Baseline at first production launch.
